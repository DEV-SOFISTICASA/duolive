# Dependencias
node_modules/

# Sessoes salvas (contem cookies/logins - NUNCA enviar ao GitHub)
conector/sessao-*.json

# Perfis fixos do postador (pasta por conta, com o login do TikTok) - NUNCA enviar
conector/perfil-postar-*/

# Videos que o usuario joga no painel pra postar - nao versionar
conector/multipost/videos/

# Capturas e historicos locais (podem conter dados de pedidos/clientes)
conector/descoberta-*.json
conector/historico-*.json
conector/produtos-cache.json
conector/precos-originais.json
conector/ensaio-*.json
conector/ensaio-*.png
conector/diagnostico-*.json
conector/diagnostico-*.png
conector/*.png

# Configuracao pessoal (URL do seu conector no Render)
conector/conector.txt

# Chave de assinatura do chat do TikTok (eulerstream.com) - NUNCA enviar
conector/chave-tiktok.txt

# Chave da IA (Google Gemini) que legenda os videos - NUNCA enviar
conector/multipost/chave-ia.txt

# Senha do painel - NUNCA enviar
conector/senha-painel.txt

# Ferramenta do tunel (binario grande) e o link temporario gerado em runtime
conector/cloudflared.exe
conector/link-atual.txt

# Dados REAIS capturados da live (nomes de compradores etc.) - PII, nunca enviar
conector/testes/dados/

# Chamadas capturadas do Chrome (contem cabecalhos da sua sessao) - NUNCA enviar
conector/chamada-*.json

# Cofre das sessoes: endereco do repositorio privado, copia local do cofre
# e as sessoes antigas que o "baixar" guarda por seguranca.
conector/cofre.txt
conector/.cofre-nuvem/
conector/sessoes.cofre
conector/*.anterior

# Postador: prints e relatorios das postagens (mostram contas logadas) - nunca enviar
conector/multipost/postar-logs/

# Configuracao local do Claude Code
.claude/

# Chave secreta do Supabase - NUNCA enviar
conector/chave-supabase.txt

# Chave secreta do Supabase do LiveDash (projeto antigo) - NUNCA enviar
conector/chave-livedash.txt
previa-login-flores.html
conector/flash-sale-capturado.json
conector/detail-full.json
conector/diagnostico-oferta.json
conector/ensaio-oferta-relatorio.json
flash-sale-capturado.json
